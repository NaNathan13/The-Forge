# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Forge / Temper runtime artifacts
.claude/temper-continue-*.md
.claude/forge-continue.md
.claude/temper-summary-*.md
.claude/token-usage.jsonl
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.playwright-mcp/
screenshots/
/tmp/forge-*.sh

# P2 single-session resilience — runtime dirs + logs (gitignored).
# .forge/resilience.config is committed config and is NOT ignored — only the
# runtime state under .forge/ is.
.forge/continuation/
.forge/heartbeat/
.forge/watchdog.log
.forge/launchd-*.log

# Tinker scratch dirs (throwaway exploratory work)
.tinker/

# The Forge's own dev files (not part of the template)
.forge-dev/

# Dependency / build (uncomment per project)
# node_modules/
# dist/
# build/
# target/
# .next/
# .turbo/
